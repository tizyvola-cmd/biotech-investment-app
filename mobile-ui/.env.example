# URL API remoto (VPS). Vuoto = stesso origin (hosting v3) o proxy dev :5174 → :8765
# VITE_API_BASE=

# Prefill schermata connessione (non forza la connessione)
VITE_DEFAULT_REMOTE_HOST=http://91.99.15.48:8765
